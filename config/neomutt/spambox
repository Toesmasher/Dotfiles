# vi: set ft=neomuttrc :
set from = "spambox@toesmasher.se"
set real_name = "Niklas Berggren"
set my_pass = "`pass email/spambox`"
set my_secret = "`pass email/toesmasher.se-secret`"
set my_smtp_refresh_token="`pass email/spambox-token`"

set folder = "~/Mail/spambox"
set mbox_type = "Maildir"

# SMTP
set smtp_url = "smtps://smtp.gmail.com:465"
set smtp_user = "spambox@toesmasher.se"
set smtp_authenticators="oauthbearer"
set smtp_oauth_refresh_command="python ~/.config/neomutt/oauth2.py \
  --quiet \
  --client_id=541987573942-5us1b6pi8j3psa9i101e0s9hbcc79icp.apps.googleusercontent.com \
  --client_secret=$my_secret \
  --refresh_token=$my_smtp_refresh_token"

set spool_file = "+INBOX"
set record = "+[Gmail].Sent Mail"
set trash = "+[Gmail].Bin"
set postponed = "+[Gmail].Drafts"

mailboxes `find ~/Mail/spambox -type d -not -name "spambox" -not -name cur -not -name new -not -name tmp -printf "=\"%P\" "`

source $HOME/.config/neomutt/common
